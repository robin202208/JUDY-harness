[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "judy-harness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for JUDY"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "DeepSeek" }]
dependencies = [
  "pydantic>=2.12,<3",
  "judy-harness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/robin202208/JUDY-harness"
Documentation = "https://github.com/robin202208/JUDY-harness/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/robin202208/JUDY-harness/issues"
Source = "https://github.com/robin202208/JUDY-harness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/judy_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
judy-harness-runtime-bin = { path = "../sdk-runtime", editable = true }
